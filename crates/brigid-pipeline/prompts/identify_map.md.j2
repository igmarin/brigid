You are analyzing batch {{ batch_idx }}/{{ batch_total }} of the monorepo `{{ project_name }}`.

Module(s) in this batch: {{ module_note }}

This codebase may be structured like loosely-coupled engines / umbrella apps
(each app under apps/<name>, packages/, engines/, etc.). Focus on THIS batch only.

Codebase Context (this batch only):
{{ context }}

{{ language_instruction }}Identify up to {{ per_batch }} important abstractions from THIS batch that would help a newcomer.
Prefer:
- Domain / business capabilities
- App or package boundaries and public APIs
- Cross-cutting infrastructure only if central to understanding the system

For each abstraction, provide:
1. A concise `name`{{ name_lang_hint }}.
2. A beginner-friendly `description` (~80-100 words) with a simple analogy{{ desc_lang_hint }}.
3. `file_indices` using global indices from the listing (format `idx # path`).
4. A `tier` — one of `S` (small/leaf), `M` (medium/core), or `L` (large/hub).
5. A `kind` — a free-form label such as `module`, `class`, `service`, `config`, `function`, `documentation`, or `utility`.
6. (Optional) `apps` — monorepo app names touched by this abstraction.
7. (Optional) `entry_files` — the best 1-3 file paths to open first.

List of file indices and paths in this batch:
{{ file_listing }}

Format the output as a YAML list:

```yaml
- name: |
    Example Abstraction{{ name_lang_hint }}
  description: |
    What it does and why it matters.{{ desc_lang_hint }}
  file_indices:
    - 0 # path/to/file.ex
  tier: "M"
  kind: "module"
  apps: []
  entry_files: []
```
